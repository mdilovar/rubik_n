done so far (current commit):
//disabled pan // made some path changes so it works in kodind env (see if compatible with cloud9)//added some congrats amination in cube.js plus congrats messages in game.js//fixed canvas size conflict with menu: canavas height (both initial and on window resize) is set to 80% of window height now

current task:


next up:





/// more:

- add mosouse movement support (coun terclockwise shouild work too; for n>3 - all middle layers should rotate.)
- user friendly step-by step guide for solution {Before even given cube status input, just like hints. input is the scrambled cube.}

- create a prototype for detecting colors on one side of a cube from an image.
- other credentials related stuff: successfull registration ui feedback, sql injection tests, password change, password forgot, add email, othertab logout, etc, login attempts...
- adding facebook api and friends high scores.
- if image prototype works, implement video feed cube scanner
- and/or alternative manual cube status selection with center color picker
- consider dropping trackball/orbit/event-controls scripts (and implementing min req func yourself) to improve original/imported code ratio.
 

optional: 
- Make mobile friendly?




other people's cubes:
https://dl.dropboxusercontent.com/u/13195512/rubik/rubikscube.html
someone else's cube ! - http://codepen.io/theonepa1/pen/fzAli








